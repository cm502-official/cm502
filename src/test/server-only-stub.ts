// Test-only stand-in for the "server-only" package (see vitest.config.ts).
// Intentionally does nothing — tests run in Node, not a browser bundle,
// so there's nothing to guard against here.
export {};
