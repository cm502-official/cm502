/**
 * Placeholder for generated Supabase types.
 *
 * Once a real Supabase project exists and the migrations in
 * supabase/migrations/ have been applied, regenerate this file with:
 *
 *   npx supabase gen types typescript --project-id <project-ref> \
 *     > src/lib/supabase/types.ts
 *
 * Until then this loose type keeps the app compiling without lying about
 * column-level type safety.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
